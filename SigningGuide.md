# 爱思助手手动签名安装教程（Windows）

CI 产出的是**未签名 IPA**，不能直接安装。下面用「爱思助手」在 Windows 上用你的 Apple ID 重新签名并安装到 iPhone。

> 免费 Apple ID 签名有效期为 **7 天**，到期需重新签名。若你有付费开发者账号，可在爱思中选择「证书签名」并导入 `.p12` 与描述文件，有效期更长。

## 准备工作

1. 一台 Windows 电脑（已装爱思助手：[https://www.i4.cn](https://www.i4.cn)）。
2. 一根数据线，将 iPhone 连接电脑，并在手机上「信任此电脑」。
3. 从 GitHub 仓库 **Actions → 最近一次成功构建 → Artifacts** 下载 `VideoCompressor-unsigned-ipa`（即 `VideoCompressor-unsigned.ipa`）。
4. 一个 Apple ID（邮箱 + 密码）。若开启了两步验证，请使用 **App 专用密码**（appleid.apple.com → 安全 → 生成）。

## 签名并安装

1. 打开**爱思助手**，等待识别到你的设备。
2. 顶部进入 **「工具箱」**，找到并打开 **「IPA 签名」**。
3. 点击 **「添加 IPA 文件」**，选择下载好的 `VideoCompressor-unsigned.ipa`。
4. 在右下角签名方式选择 **「使用 Apple ID 签名」**，点击 **「添加 Apple ID」**，输入你的 Apple ID 与密码（或 App 专用密码）。
5. 选中刚才添加的 Apple ID，点击 **「开始签名」**。
   - 显示「签名成功」后即可安装。
6. 回到爱思助手主界面，进入 **「我的设备 / 应用游戏」** 或 IPA 签名界面的 **「立即安装」**，把签名后的应用安装到手机。

## 在 iPhone 上信任开发者

首次打开 App 可能提示「未受信任的开发者」：

- 打开 iPhone **设置 → 通用 → VPN与设备管理（或“描述文件与设备管理”）**
- 在「开发者应用」下找到你的 Apple ID，点击 **「信任」**。

之后即可正常打开「视频压缩」App。

## 常见问题

- **签名失败 / 账号错误**：确认 Apple ID 与密码正确；开启两步验证请改用 App 专用密码。
- **安装后闪退**：描述文件未信任，按上面步骤在「设置」中信任开发者；或签名已过期（免费 7 天需重签）。
- **爱思识别不到设备**：换原装数据线、重新插拔、在手机上确认「信任此电脑」。
- **提示设备不兼容**：确保 iPhone 系统 ≥ iOS 16.0。

## 说明

- 此方式仅在你自己的设备上安装，不依赖任何云签名服务。
- 本项目不收集任何信息，签名过程使用的 Apple ID 仅由爱思与苹果服务器交互，不会上传视频。
