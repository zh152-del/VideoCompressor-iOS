# 2026-08-25

## iOS 本地视频压缩 App（VideoCompressor-iOS）
- 本地 commit `26aa365`（53 文件，纯 SwiftUI + AVFoundation，完全离线，无第三方运行时依赖）已成功推送到 `github.com/zh152-del/VideoCompressor-iOS` 的 `main` 分支，`default_branch=main`。
- 推送用 **Classic PAT**（`ghp_` 前缀，含 `repo` + `workflow` scope）成功。
- 此前失败路径：3 个 fine-grained PAT 全部失效/只读（401/403/401）；已连接的 GitHub MCP 连接器仅只读（写入返回 403 Resource not accessible by integration）。结论：走 Classic PAT 最稳。
- 已触发 GitHub Actions CI（run id `32845419693`，`macos-14`）：xcodegen 生成工程 → `xcodebuild test`（模拟器 iPhone 15）→ `xcodebuild build` Release（`CODE_SIGNING_ALLOWED=NO`）→ 打包 `VideoCompressor-unsigned.ipa` → 上传 artifact。
- 正在等待 CI 结果，准备据真实日志修复编译/测试错误。
- 交付路线：未签名 IPA + 爱思手动签名（`SigningGuide.md` 已写）。明确红线：未签名 IPA 无法在普通 iPhone 直接安装，必须爱思重签。

## CI 真实编译里程碑（已绿）
- 经多轮「真实日志 → 修复 → 重推」闭环，CI 最终 **green build**：run `32848752503`（commit `c92fa95`，`macos-15` / Xcode 16.4 / iOS 18.5 SDK）。
- 关键修复链（均来自真实 CI 日志，非猜测）：
  1. CI runner `macos-14`(Xcode15.4) 读不了 xcodegen 生成的格式77工程 → 改 `macos-15`。
  2. `SWIFT_VERSION: "5.0"` 无 async/await → 两处改 `5.10`。
  3. `await writer.finishWriting()` 在 iOS18.5 SDK 实为 async（需 await）→ 保留 await。
  4. 测试目标 `iPhone 15` 在 macos-15 runner 不存在 → 改 `iPhone 16,OS=18.5`。
  5. 多处类型不匹配（Float/Double、Int/Int64）→ 修正。
  6. `PHPhotoLibrary.performChanges` 在实例上调用 → 改 `PHPhotoLibrary.shared().performChanges(...)`。
  7. `TaskStatus == .pending` 需 Equatable → 加 `isPending` 属性替代 `==`。
  8. `AVAssetTrack.formatDescriptions`（已废弃，返回 `[Any]`）歧义 → 改 `load(.formatDescriptions)`（返回 `[CMFormatDescription]`）。
- **产物已验证**：artifact `VideoCompressor-unsigned-ipa`（382,309 字节）；结构 `Payload/VideoCompressor.app`，含 ARM64 Mach-O（`cputype=0100000c`）二进制；**无 `_CodeSignature`、无 `embedded.mobileprovision`** → 确为未签名 IPA。
- 本地校验副本：`C:\Users\Administrator\Desktop\ios开发2\ipa_check\VideoCompressor-unsigned.ipa`。
- 诚实边界：Windows 下无法真机安装/运行/验证 UI 与压缩效果；未签名 IPA 必须经爱思（或开发者证书）重签才能在 iPhone 安装。
